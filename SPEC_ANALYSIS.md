# Paperback SPEC.md 补充分析要点

本文档基于对 `SPEC.md` 的评审，识别出以下潜在的性能瓶颈和被忽略的边缘场景，供进一步分析和讨论。

---

## 1. 技术可行性与性能瓶颈

### 1.1 `cardsInfo` 批量获取的性能问题
- **现象**: Spec 定义在生成默写卷时，通过一次 `cardsInfo` API 调用获取所有卡片（limit 最高 100）的完整 HTML 内容。
- **潜在风险**: AnkiConnect 是在 Anki 主进程中运行的插件。单次请求如果需要处理大量数据（例如 100 张包含高清图片或大段代码的卡片），可能会消耗 Anki 大量的 CPU 和内存，导致 Anki 桌面客户端出现短暂的卡顿或无响应，影响用户体验。

### 1.2 `answerCards` 逐个调用的网络开销
- **现象**: Spec 定义在批改时，每评一张卡就调用一次 `answerCards` API 将结果实时写回 Anki。
- **潜在风险**: 如果一共有 50 张卡片需要批改，这将导致 50 次独立的 HTTP 请求。虽然单次请求的数据量很小，但大量请求的累计网络往返时间（RTT）可能会造成可感知的延迟，尤其是在网络状况不佳时。这与用户快速评分的直觉操作可能存在冲突。

---

## 2. 盲点扫描与边缘场景 (Edge Cases)

### 2.1 Anki Profile 切换导致的状态不一致
- **场景**: 用户在 Paperback 中生成了默写卷（Session A，属于 Profile A），然后在 Anki 桌面端切换到了另一个用户 Profile B。当用户回到 Paperback 继续批改 Session A 时，`answerCards` 请求会发送给当前活动的 Profile B。
- **潜在风险**:
    1.  **写回失败**: Profile B 中不存在 Session A 的 card_id，导致所有评分请求都失败。
    2.  **数据错乱 (罕见但致命)**: 如果 Profile B 中恰好存在与 Session A 相同的 card_id，评分数据会被错误地写入不相关的卡片。

### 2.2 默写期间 Anki 数据被外部修改
- **场景**: 用户生成默写卷后，但在完成批改前，通过 Anki 的同步功能、手动编辑、或其它插件删除了默写卷中的某些卡片或牌组。
- **潜在风险**: 对已删除卡片的 `answerCards` 调用会失败。Spec 提及了对写回失败的处理，但需要明确此场景下的用户提示，避免用户困惑。

### 2.3 并发操作导致的文件写争用 (Race Condition)
- **场景**: 用户在浏览器中意外地打开了两个或多个指向同一个 Session 的批改页面 (e.g., `.../session/<id>/grade`)。
- **潜在风险**: 当用户在不同页面中为不同卡片评分时，多个进程会几乎同时读取 `session.graded` JSON 文件、在内存中修改、然后写回。这会大概率导致后写入的操作覆盖掉先前的写入，造成评分数据丢失。

### 2.4 对非标准 Anki Note Type 的解析脆弱性
- **场景**: Spec 假设“卡片的第一个字段是正面，第二个字段是背面”。
- **潜在风险**: 这个假设对于用户自定义的、或从网上下载的复杂 Note Type (如 Image Occlusion, 或字段顺序不标准的模板) 很容易失效。可能导致：
    - 答案被错误地显示在默写卷上。
    - 无关的提示信息被当作问题或答案。
    - 程序因找不到足够字段而崩溃。

### 2.5 系统级权限与路径问题
- **场景**: 程序尝试在 `~/.paperback/` 或 `PAPERBACK_DATA_DIR` 指定的目录中创建 Session 文件。
- **潜在风险**: 如果程序因为权限问题无法写入目标目录，或者该目录本身不存在且无法被创建，会导致 Session 创建失败。需要在程序启动或创建时进行检查和清晰的用户提示。

---

## 3. 一阶段代码评审结论：风险验证 (2026-06-23)

本节旨在验证 §1-§2 中预测的风险在当前代码中的实际状态。

**核心结论**: 经过对代码库的详细审查，先前预测的绝大部分关键风险点（并发、权限、外部状态变化等），**实际上都已通过健壮的机制得到了处理**，并拥有测试覆盖。

### 3.1 已验证被修复的风险

*   **并发写争用**:
    *   **验证**: 风险**已解决**。`session.py` 中的 `mark_graded`, `mark_pending`, `mark_invalid` 方法实现了原子化更新模式：在锁范围内重新从磁盘读取最新状态，合并修改，然后写回。
    *   **证据**: `session.py` mark_* 方法；`tests/test_session.py::test_concurrent_writes_no_loss` 覆盖了并发写入不丢数据。

*   **Note Type 解析脆弱性**:
    *   **验证**: 风险**已解决**。代码通过 `anki_connect.py` 的 `SUPPORTED_NOTE_TYPES` 白名单过滤，而非“软失败”。
    *   **证据**: `anki_connect.py` `SUPPORTED_NOTE_TYPES`。不属于 `Basic` / `Cloze` 的卡片被主动跳过，并在前端提示“已跳过 N 张不支持的类型”，避免崩溃或暴露技术错误。

*   **系统权限问题**:
    *   **验证**: 风险**已解决**。应用启动时执行 `ensure_data_dir_writable()` 预检查。
    *   **证据**: `session.py::ensure_data_dir_writable`、`main.py` 启动校验。数据目录不可写时不崩溃，而在首页显示清晰提示。

*   **Anki Profile 切换**:
    *   **验证**: 风险**已解决**。批改开始前 `_verify_deck()` 校验当前 Anki profile 的 deck 名与 Session 记录是否一致。
    *   **证据**: `main.py::_verify_deck`（`SPEC.md §6.5`）。不一致拦截批改并提示，防止写错 profile。

*   **默写期间数据被外部修改**:
    *   **验证**: 风险**已解决**。`grade_card` 路由对写回结果做精细化错误处理。
    *   **证据**: `main.py::grade_card`。区分“卡片在 Anki 中已不存在”（`mark_invalid` 跳过）与“临时网络/AnkiConnect 错误”（`mark_pending` 待重试），落实 `SPEC.md §6.4` 设计。

### 3.2 有意为之的设计决策

*   **性能瓶颈**:
    *   **验证**: 此项为**有意为之的设计取舍**，而非疏忽。
    *   **证据**: `SPEC.md §9.1` 明确：考虑到 localhost 极低延迟（RTT < 5ms）与 `cardsInfo` 纯文本传输效率，v1 为实现简单与实时性，接受当前的调用方式。

**总结**: 当前代码库的健壮性已覆盖 §2 预测的关键执行风险。
